# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/

# Virtualenvs
.venv/
venv/
env/

# IDE / editor
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Pipeline runtime artifacts (these live in <project>/ trees, not here, but guard anyway)
*.duckdb
*.duckdb.wal
portfolio.duckdb*

# Local config that should not be checked in
.env
.env.local
*.local.json

# Concrete queue files (only the template ships with the repo)
lit_pull_queue.csv
lit_pull_queue.*.processed*.csv
lit_pull_queue.draft.csv
projects.json

# Internal / cross-session docs — NEVER publish (leak portfolio structure, paths, audit notes)
CURRENT_STATE.md
_PORTFOLIO_BRIEF.md
LOOSE_ENDS.md
POLISH_PUNCHLIST.md
notes/
# Research scratch (heavy absolute-path + project-name leaks; not portfolio content)
eval_pdf_abstract_heuristic_2026-05-22/
