# Exact-version lockfile for reproducibility.
# Generated against Python 3.13.12 on Windows 11 + miniconda3, 2026-05-13.
# This is the version set that CI green-lights and that the maintainer runs.
#
# Use:  pip install -r requirements.lock.txt
# For looser bounds (recommended for new installs):  pip install -r requirements.txt

requests==2.32.5
duckdb==1.5.2
pymupdf==1.27.2.2
pdfplumber==0.11.9
